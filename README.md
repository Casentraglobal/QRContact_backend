# Auth Backend

Express + MongoDB auth API with local, Google, Apple, and generic SSO skeleton.

Setup

1. Copy `.env.example` to `.env` and fill values.
2. Install deps:

```powershell
cd "d:/QR project/auth-backend"
npm install
```

3. Run dev:

```powershell
npm run dev
```

API endpoints available under `/api/auth`.
